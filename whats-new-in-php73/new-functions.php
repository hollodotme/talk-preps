<?php declare(strict_types=1);


array_key_first();                  // Get the first key of an array
array_key_last();                   // Get the last key of an array
hrtime();                           // Monotonic high resolution system time
is_countable();                     // Check for a countable struct

DateTime::createFromImmutable();    // \o/